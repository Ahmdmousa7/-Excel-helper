# Technical Debt Register

Living document. Every item keeps its ID for life — closed items move to the bottom, they are never deleted, so a recurrence can be traced to its original entry.

**Severity:** Critical (exploitable / data loss now) · High (users hit it, or it blocks other work) · Medium (real, has a workaround) · Low (cost without user impact).

**Status:** `open` · `in-progress` · `blocked` · `accepted` (deliberate, with a stated reason) · `closed`.

## Open

| ID | Description | Sev | Risk | Effort | Owner | Status | Blocked by | Fix recommendation |
|---|---|:---:|---|:---:|---|---|---|---|
| **TD-030** | `verify-evidence.mjs` verifies the attestation signature against **only the first identity** in `allowed_signers`. With two or more registered signers, a signature from any but the first cannot verify. | Low | **Fails closed, not open** — an unverifiable signature blocks the gate; it never wrongly passes. No signer is registered today, so the path is unexercised | S | — | `open` | Nothing — deferred on ROI, not blocked | **Deferred deliberately.** Fixing it now is speculative work on a code path with no signers, no way to test realistically, and no user or business impact. **Revisit at TD-028's trigger** (enabling signing / a second contributor) — and this entry exists so that whoever enables signing does not discover it works for one person only. Fix is to iterate the identities, or read the signer from the signature envelope |
| **TD-031** | Three cosmetic review findings left unfixed on purpose: `evidence.mjs` trusts the attestation's recorded `attestation_id` without recomputing it (the verifier recomputes, so it is redundant); `collectors.mjs` had a dead re-export; a redundant `.map().sort()` before `.length`. The latter two were fixed as they were one-line and zero-risk; the rehash was not. | Low | None. The verifier recomputes the digest, so a corrupt attestation is caught one step later with a clearer message | S | — | `accepted` | — | **Accepted, not scheduled.** Adding a second rehash is defence-in-depth against a case the verifier already covers. Per the ROI policy: a change with zero user impact still carries a nonzero chance of introducing a defect, and this session produced direct evidence of that rate. Revisit only if the generator ever becomes the sole reader of the attestation |
| **TD-029** | `build:e2e` and `build` shared `dist/`, and `vite preview` serves that directory live from disk — so a production build silently replaced what the e2e server was serving. With `reuseExistingServer` on, this produced **100 of 101 Playwright failures**, all "element not found", because the served bundle had been rebuilt without `VITE_E2E_AUTH_BYPASS` and every test hit the sign-in gate. | **High** | Fixed, but recorded because the failure signature is indistinguishable from a real app regression and cost real time to diagnose twice | S | — | `closed` | — | Fixed: `build:e2e` emits to `dist-e2e/`, preview serves that, and `reuseExistingServer` now defaults to **off** (`PW_REUSE_SERVER=1` to opt in). The collision is structurally impossible rather than merely unlikely |
| **TD-028** | The review attestation's SSH signature is **optional and currently unused** — no key is registered in `.apexyard/allowed_signers`, so CI accepts it unsigned ([ADR-0002](../adr/ADR-0002-review-attestation.md)). | Low | None today: the signature would prove *which key* attested, and there is one maintainer holding both the key and the thing being attested. It becomes load-bearing the moment a second contributor can push | S | — | `accepted` | Nothing — it is one command away | Deliberately not enabled: a signature that only proves the maintainer signed their own claim is ceremony. **Enable at the same trigger as TD-027 (a second regular contributor):** `ssh-keygen -t ed25519`, append `<email> <pubkey>` to `.apexyard/allowed_signers`, and CI starts requiring a valid signature automatically — the verifier already fails on a missing or bad one once signers exist, and that path is tested |
| **TD-027** | CI can verify that a review **covering this exact code** was recorded, but **not that a model was actually invoked**. The review runs only on the maintainer's machine ([ADR-0001](../adr/ADR-0001-ai-review-runs-locally-not-in-ci.md)) and emits a content-bound attestation that CI checks ([ADR-0002](../adr/ADR-0002-review-attestation.md)). The attestation is produced by the same machine that could fabricate it. | Low | A maintainer who deliberately hand-writes an attestation passes CI. Accidental staleness — the failure that actually happens — is now caught: an edit after review, an unreviewed added file, or a resurrected deletion all fail the `Review attestation` gate | S | — | `accepted` | Unforgeable-to-the-operator is impossible for a local process; see ADR-0002 § "The honest framing, first" | **Was Medium, now Low** — the attestation closed the accidental half, which was the part that had real probability. What remains is deliberate forgery by the one person who would gain nothing from it. Two mitigations are in place, not deferred: `.githooks/pre-push` runs the full gate on every push (`npm install` configures it), and the pre-push hook re-verifies the attestation with the same code CI runs. Both are local and both are bypassable with `--no-verify` — a bypass is now a decision rather than an oversight. **Revisit trigger: a second regular contributor**, at which point enable signing (TD-028) |
| **TD-026** | The Firebase Web API key ships in `firebase-applet-config.json` and the client bundle — correct and unavoidable, it is a public project identifier rather than a credential. `firestore.rules` **is** committed and is deny-by-default (no wildcard match; `/users/{userId}` readable only by its owner or an admin; role escalation blocked on both create and update). Two narrower things remain unverified: whether that ruleset is **deployed** to the live project, and whether HTTP-referrer restrictions exist on the key. | Low | A committed rules file is not a deployed one — if the project still runs an earlier or default ruleset, the published key reaches whatever that permits. Referrer restrictions are defence-in-depth for quota abuse, not access control, since Firestore access is governed by the rules. | S | — | `open` | Needs Firebase console access | `firebase deploy --only firestore:rules` and record the deploy, so the reviewable file in git and the live ruleset are known to match; then add referrer restrictions scoped to `ahmdmousa7.github.io`. **Originally filed as High on the assumption that no rules existed — that was wrong, the file was in the repo the whole time. Corrected on review.** |
| **TD-034** | `gateSatisfied` never looks at `verdict`. An attestation carrying `"verdict": "REQUEST_CHANGES"` passes CI as long as `critical` and `high` are zero — reachable when a reviewer requests changes over a Medium or a handbook rule, since `review-local.sh` gates only on `--fail-on <severity>`. `attest-review.mjs` widens it at the other end by defaulting an absent verdict to `unknown`. | Medium | A review that explicitly said "do not merge" can still go green. Bounded: the severity counts still gate independently, so it takes a request-changes with no high/critical finding | S | — | `open` | Nothing — deferred on ROI | **Deferred, not dismissed.** The fix is a few lines (treat `REQUEST_CHANGES` as failing, reject `unknown`), but it changes what the hard gate rejects, and getting it wrong blocks every push. Wants its own change with its own test round rather than riding along with a production hotfix. Found reviewing the .nojekyll fix, 2026-08-03 |
| **TD-035** | `parseAttestation` does not verify that `files` is sorted, though `comparePathsBytewise` exists specifically so the order is reproducible across tools. An out-of-order array yields a self-consistent digest and passes `isCanonical`, so only the producer enforces the invariant the format documents. | Low | None today — `buildAttestation` always sorts. Becomes real the moment a second producer exists, which is the same trigger as [TD-028] | S | — | `open` | Nothing — deferred on ROI | Assert the order in `parseAttestation`, one loop. Revisit at TD-028's trigger, alongside TD-035's sibling TD-036 |
| **TD-036** | `attest-review.mjs` derives the bundle directory as `dirname(opt.out)`, so `--out attestation.json` (no directory component) yields `'.'`, and `isBundlePath(p, '.')` matches nothing. The bundle-exclusion rule silently becomes a no-op and every bundle artifact is pulled back into the attested scope — the same class of defect the comment above it warns about, reached through a CLI flag instead of a duplicated list. | Low | Unreachable from any committed caller: `review-local.sh` never passes `--out`, and the default carries a directory. A hand-run with a bare filename would produce an attestation that can never verify | S | — | `open` | Nothing — deferred on ROI | Reject an `--out` with no directory component, or default the dir to `BUNDLE_DIR`. Found reviewing the .nojekyll fix, 2026-08-03 |
| **TD-025** | No cross-browser or visual-regression coverage. Suite is Chromium-only. | Medium | Engine-specific rendering bugs (Safari/WebKit especially) and unintended visual changes ship unseen | M | — | `open` | Needs a decision on baseline management | Nightly (not per-PR): webkit + firefox roughly triples e2e time, and visual baselines are platform-specific — they must be generated in the CI container, not locally, or every run diffs against Windows-rendered text |
| **TD-023** | The Web Scraper routes user-supplied URLs through five **public, third-party CORS proxies**: `api.allorigins.win`, `corsproxy.io`, `cors-anywhere.herokuapp.com`, `thingproxy.freeboard.io`, `r.jina.ai`. | **High** | Every scraped URL and its full response passes through infrastructure nobody here controls, logs, or has an agreement with — a silent data-egress path. `cors-anywhere.herokuapp.com` is also dead (Heroku ended free dynos), so that path fails outright. Separately, this is why `connect-src` cannot be tightened: there is no finite host list when the user supplies the target. | M | — | `open` | — | Replace with one controlled proxy (Cloudflare Worker / Vercel function) that this project owns. Then `connect-src` becomes enumerable and the CSP can be tightened in the same change. |
| **TD-022** | `xlsx-js-style@1.2.0` is built on the vulnerable SheetJS 0.18.5 base. Kept because it is **write-only** — audited every call site: `utils`/`write`/`writeFile`, zero `read`. | Low | None on the current call sites. Becomes Critical the moment anyone calls `.read()` on it. | S | — | `accepted` | No maintained styled-write alternative on npm | **Guard rail needed:** add a lint rule or CI grep banning `XLSX_STYLE.read`. Revisit if styled *reading* is ever required. |
| **TD-002** | No dependency-audit gate. 25 vulnerabilities (4 critical, 7 high) accumulated unnoticed across 854 packages. | **Critical** | The next critical CVE also lands silently | S | — | `in-progress` | — | `npm audit` job failing on high+ for production deps; weekly scheduled scan |
| **TD-003** | Gemini API key is inlined into the browser bundle by `VITE_` prefix. | **High** | Public site → key is readable in devtools → billing drain | S (mitigate) / L (fix) | — | `open` | — | Now: referrer restriction + hard quota cap. Later: proxy endpoint |
| **TD-024** | `aiServiceFactory` was imported into `App.tsx` and never used — 52 KB gzipped of `@google/genai` in the entry for nothing. Removed, but nothing prevents the next one. | Low | Dead imports of heavy libraries silently inflate the initial load; `no-unused-vars` is a warning here, not an error | S | — | `open` | — | Promote `@typescript-eslint/no-unused-vars` to error for **import declarations only**, once the 214 existing unused-var warnings are cleared (TD-014 closed the bulk) |
| **TD-005** | Sidebar is a fixed `w-64` with no mobile collapse. 22 header controls unreachable below ~700px. | **High** | The app is unusable on a phone | M | — | `open` | — | Off-canvas drawer below `md` + hamburger. Ratchet: `KNOWN_MOBILE_OVERFLOW_BUDGET` 24 → 0 |
| **TD-006** | No Content-Security-Policy. Tailwind loads from a CDN `<script>`, which also forces `unsafe-eval`. | **High** | No defence-in-depth against injected script | M | — | `in-progress` | TD-007 | Move Tailwind into the build, then add a `<meta>` CSP |
| **TD-007** | Tailwind compiled in-browser from `cdn.tailwindcss.com`. Explicitly not for production. | **Medium** | Style flash, CDN as a hard dependency, blocks CSP | S | — | `open` | — | `npm i -D tailwindcss postcss autoprefixer`; move config to `tailwind.config.js` |
| **TD-008** | API-key modal is a keyboard trap: no `role="dialog"`, no Escape, no focus trap or restore. Auto-opens on first load. | **Medium** | First thing a keyboard user meets, and they cannot leave it | S | — | `open` | — | Add dialog semantics, Escape handler, focus management |
| **TD-009** | 70 form controls with no label association (`jsx-a11y/label-has-associated-control`). | **Medium** | Screen-reader users cannot tell what a field is for | M | — | `open` | — | Mechanical `htmlFor`/`id` pass, ~10 files at a time |
| **TD-010** | `VariableBalanceTab` (1,383 LOC) and `VariableBalanceTabV2` both in tree; only one routed. | **Medium** | Fixes land in the unrouted copy | S | — | `open` | Product decision on which is correct | Land the migration or delete V2 |
| **TD-011** | Three components over 1,000 LOC (1,383 / 1,369 / 1,057). | **Medium** | Unreviewable diffs; every change is high-risk | XL | — | `open` | — | Extract logic to `utils/`, split by tab section. Add a size gate at 800 LOC for *new* files only |
| **TD-012** | 290 `any` annotations. External data (`JSON.parse`, sheet parses, model responses) typed by assertion. | **Medium** | Runtime `undefined is not a function` in a data-processing app | L | — | `open` | — | Validate at the four boundaries first; leave interior `any` alone |
| **TD-013** | 27 `react-hooks/exhaustive-deps` warnings. | **Medium** | Stale closures — the hardest class of bug to reproduce | M | — | `open` | — | Triage individually; each is either a real bug or needs a documented disable |
| **TD-014** | 214 unused variables/imports. | **Low** | Noise; hides genuinely dead code | S | — | `open` | — | `npm run lint:fix` in one dedicated PR |
| **TD-015** | ~20 scratch scripts at repo root (`test-site*.js`, `output.txt`, …). | **Low** | Noise in every listing and diff | S | — | `open` | — | Delete; git history retains them |
| **TD-016** | `stream-browserify` declared as a runtime dependency, never imported. | **Low** | Dead weight in the dependency tree | S | — | `in-progress` | — | Remove from `package.json` |
| **TD-017** | Playwright is chromium-only; no visual regression, no offline/large-file/tablet coverage. | **Medium** | Engine-specific and layout regressions ship unseen | M | — | `open` | — | Page objects first, then nightly cross-browser + visual |
| **TD-018** | Components have zero unit tests; coverage scoped to `utils/**` only. | **Medium** | Component logic is only exercised end-to-end — slow, coarse feedback | L | — | `open` | — | Extract pure logic out of components, test the extraction |
| **TD-019** | GitHub Pages cannot set HTTP headers, so no `frame-ancestors`, HSTS, or `X-Frame-Options`. | **Medium** | Clickjacking cannot be prevented at the platform level | M | — | `accepted` | Platform limitation | Accepted while on Pages. Revisit if the app moves to a host that can set headers |
| **TD-020** | Devtool CVEs: `vitest` 2→4, `vite` 5→8, `@vitest/coverage-v8` — all major bumps. | **Medium** | Build-time only, but blocks a clean audit | M | — | `open` | — | Dedicated PR per major; verify build + full suite each |

## Closed

| ID | Description | Closed | Fixed by |
|---|---|---|---|
| **TD-033** | **Live regression: 28 of the app's tools could not open.** GitHub Pages runs Jekyll, which excludes paths beginning with `_`. Rollup names its CommonJS dynamic-require helper `_commonjs-dynamic-modules-<hash>.js`, so Pages served 404 for it while all 28 tool chunks kept a **static** import of it — and a static import of a missing module fails the whole chunk. Introduced by the code-splitting in `a7c551a` (PR #1, deployed 2026-08-02 17:23), which created that chunk for the first time; the missing `.nojekyll` had been harmless for every earlier deploy. CI, the deploy job, and all 18 live tests stayed green throughout: the file was present in `dist/` and in the `gh-pages` branch, and no homepage request ever touched a tool chunk. | 2026-08-03 | `public/.nojekyll` (one empty file, copied to `dist/` root by Vite). Plus a live test that walks the static module graph from the entry chunk and asserts every edge resolves — the gap that let a broken site pass 18 tests was that all of them only watch homepage traffic. |
| **TD-032** | `.apexyard/attestation.json` was a derived mirror of a canonical text manifest, costing a verification rule — *mirror agrees with manifest* — that existed only because the mirror existed. The justification for keeping the text form authoritative was that an SSH signature covered its bytes, which protected a capability with **no registered signer** (TD-028). | 2026-08-03 | [ADR-0004](../adr/ADR-0004-json-is-the-canonical-attestation-format.md): the JSON is now the attestation, the text manifest is deleted, and rule 2 became a self-check. Net −1 verification rule, −1 artifact, −2 redundant fields, and one serialiser shared with the rest of the bundle instead of two. |
| TD-C01 | `aiService.generateContent()` did not exist — "AI Analysis" threw on every click | 2026-08-02 | `674c769` |
| TD-C02 | `.gitignore` covered `.env` but not `.env.local`, where the README says to put the API key | 2026-08-02 | `95462c7` |
| TD-C03 | Two TypeScript errors; `tsc` had never gated anything | 2026-08-02 | `674c769` |
| TD-C04 | Four accessibility defects (unlabelled QR controls, unnamed copy button, `alt`-less previews) | 2026-08-02 | `674c769` |
| TD-C05 | `puppeteer` was a production dependency, pulling ~170 MB of Chromium into every install | 2026-08-02 | `95462c7` |
| TD-002 | No dependency-audit gate — 25 CVEs had accumulated unnoticed | 2026-08-02 | `b99fb68` |
| TD-014 | 214 unused variables and imports | 2026-08-02 | `7c9774a` |
| TD-015 | ~20 scratch scripts at the repo root | 2026-08-02 | `7c9774a` |
| TD-016 | `stream-browserify` declared but never imported (−32.7 KB gzip) | 2026-08-02 | `7c9774a` |
| TD-021 | Deploy ran on every push to main with no CI dependency — a red build could publish | 2026-08-02 | `160b856` |
| **TD-005** | Sidebar fixed at `w-64`; 22 header controls unreachable below ~700px | 2026-08-02 | Off-canvas drawer below `md` with hamburger + backdrop, `rtl:`-aware. Overflow **22 → 1** (the remaining one is a deliberate decorative blob). Ratchet 24 → 2. |
| **TD-008** | API-key modal was a keyboard trap — no role, no Escape, no focus management | 2026-08-02 | Reusable `useModalA11y` hook; 8 tests on a dialog nothing had ever opened. |
| **TD-017** | Playwright had no page objects; chromium-only; no offline / large-file / invalid-file coverage | 2026-08-02 | 3 page objects, real fixtures, 4 new suites. **62 → 101 tests.** Cross-browser and visual regression carried forward as TD-025. |
| **TD-001** | `xlsx@0.18.5` prototype pollution, no npm fix, on the untrusted-parse path | 2026-08-02 | `@e965/xlsx@0.20.3` behind a Vite alias. Production audit went from 5 high/critical to **zero**. Guard rail added so `xlsx-js-style` can never become a parser (TD-022). |
| TD-006 | No Content-Security-Policy | 2026-08-02 | Meta CSP with `object-src 'none'`, `base-uri 'self'`, `form-action 'self'` enforced. Caught and fixed a `fetch()`-on-`data:`-URL break in QR copy on the way in. |
| TD-007 | Tailwind compiled in-browser from a CDN | 2026-08-02 | PostCSS build pipeline, Tailwind 3.4 (v3-lts). CSS now 10.9 KB gzip and budgeted. |
| **TD-004** | Single 3,761 KB JS chunk; no code splitting | 2026-08-02 | 28 tabs `React.lazy`, three eager vendor imports made dynamic, one dead import removed, `manualChunks` narrowed to react + firebase. **Initial load 1,252 → 199.3 KB gzipped (−84%)**, 81 chunks. |

## Rules for this register

1. **Nothing is closed without a commit SHA.** "Fixed in a later refactor" is not a close.
2. **`accepted` requires a written reason and a revisit trigger** (see TD-019).
3. **Every ratchet in the test suite maps to an entry here.** If the ratchet moves, the entry moves.
4. **New debt is added in the same PR that creates it.** Debt you chose is fine; debt you hid is not.
